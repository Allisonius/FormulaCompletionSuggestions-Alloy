<configuration>
    <property name="sessionId" value="${random}"/>
    <property name="logPattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>alloy-server-${sessionId}.log</file>
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>
    <!-- <appender name="CLIENT" class="alloy.language.server.LanguageClientAppender" />
    <root level="INFO">
        <appender-ref ref="STDERR" />
        <appender-ref ref="FILE" />
        <appender-ref ref="CLIENT" />
    </root> -->
    <logger name="kodkod.engine" level="OFF"/>
</configuration>
